name: ade-pulumi-python
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python Pulumi program in ADE
config:
  resource-group-name:
    type: string
  sku:
    type: string
    default: Standard_LRS
